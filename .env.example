# used by docker compose + act)
# Replace DOPPLER_TOKEN in your `.env` with one of these 1-line commands.
# Dev (24h): create a temporary dev token named "<hostname>-tmp" and append it
# token=$(doppler configs tokens create "<hostname>-tmp" --config dev --max-age 24h --plain) && printf 'DOPPLER_TOKEN=%s\n' "$token" >> .env
# Prod (24h): create a temporary prod token named "<hostname>-tmp" and append it
# token=$(doppler configs tokens create "<hostname>-tmp" --config prd --max-age 24h --plain) && printf 'DOPPLER_TOKEN=%s\n' "$token" >> .env

DOPPLER_TOKEN=