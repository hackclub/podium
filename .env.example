# Local dev - copy to .env (used by docker compose + act)
# Generate: doppler configs tokens create dev-local-short --config dev --max-age 1h --plain
# Generate: doppler configs tokens create dev-local-long --config dev --max-age 1440h --plain
DOPPLER_TOKEN=
