# Playwright E2E Test Environment Variables
# Backend automatically runs with 'doppler run --config dev' (handled by Playwright config)
# No manual configuration needed if Doppler CLI is logged in (doppler login)

# Optional: API URL (defaults to http://127.0.0.1:8000)
PUBLIC_API_URL=http://127.0.0.1:8000
