# Playwright E2E Test Environment Variables
# Copy to .env and fill with your development values
# Or use Doppler: doppler run -- bun run test:e2e

# JWT Configuration (must match backend development config)
PODIUM_JWT_SECRET=your-development-jwt-secret-here

# Optional: API URL (defaults to http://127.0.0.1:8000)
PUBLIC_API_URL=http://127.0.0.1:8000
