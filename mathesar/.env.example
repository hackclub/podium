# Local development only
# Copy to .env and fill in values

# Option 1: Use Doppler (recommended)
# Generate a token: doppler configs tokens create --max-age 1h --plain
DOPPLER_TOKEN=

# Option 2: Set secrets directly (no Doppler)
# Uncomment and set these if not using Doppler:
# POSTGRES_PASSWORD=localpass
# MATHESAR_ADMIN_PASSWORD=admin123
# PODIUM_DATABASE_URL=postgresql://postgres:localpass@host.docker.internal:5432/podium
